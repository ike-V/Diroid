import SwiftUI

extension Color {
    /// Official Android brand green (#3DDC84, source.android.com), used for folder icons
    /// throughout the browser to match the app's own icon.
    static let androidGreen = Color(red: 0x3D / 255.0, green: 0xDC / 255.0, blue: 0x84 / 255.0)
}
